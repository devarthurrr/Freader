services:
  freader:
    build: .
    container_name: freader
    restart: unless-stopped
    ports:
      - "3000:3000"
    volumes:
      - ./data:/app/data
    environment:
      - PORT=3000
      - COMICVINE_API_KEY=${COMICVINE_API_KEY:-}
